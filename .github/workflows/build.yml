name: Build

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    name: Build

    steps:
      - name: Clone repository
        uses: actions/checkout@v4

      - name: Build image
        run: |
          docker build . \
            --no-cache \
            --tag wp-veritas
