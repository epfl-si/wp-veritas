- name: wp-veritas secrets
  openshift:
    state: latest
    resource: Secret
    name: "{{ wp_veritas_secret_name }}"
    namespace: "{{ openshift_namespace }}"
    content: |
      apiVersion: v1
      stringData:
        mongo-url: toto
      kind: Secret
      metadata:
        name: "{{ wp_veritas_secret_name }}"
        namespace: "{{ openshift_namespace }}"
      type: Opaque
